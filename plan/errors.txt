Ошибка очень характерная:

Error:   x Expected '</', got ':'


Сборщик (TSX-компилятор) думает, что он парсит JSX, а натыкается на : из строчки интерфейса.
Это почти всегда значит, что у тебя интерфейс AssignCategoryModalProps случайно объявлен внутри JSX-блока или в месте, где TS ждёт HTML/JSX.

Где проблема

Фрагмент у тебя такой:

152 | import { iconMap } from '@/components/categories/CategoryCard'
153 |
154 | interface AssignCategoryModalProps {
155 |   isOpen: boolean


То есть на первый взгляд всё ок. Но раз TS ругается «Expected </», значит:

либо перед строкой 154 где-то JSX не закрыт (</div>, ), ``}`, …),

либо interface ... объявлен внутри тела функции/компонента, а не на верхнем уровне файла.

Как исправить

Убедись, что interface AssignCategoryModalProps объявлен вне компонента, обычно сразу после импортов:

import { iconMap } from '@/components/categories/CategoryCard'
import { UncategorizedExpenseWithKeywords } from '@/types/expenses'  // пример

interface AssignCategoryModalProps {
  isOpen: boolean
  onClose: () => void
  onSuccess: () => void
  expense: UncategorizedExpenseWithKeywords
}


Потом уже идёт твой компонент:

export default function AssignCategoryModal({
  isOpen,
  onClose,
  onSuccess,
  expense,
}: AssignCategoryModalProps) {
  // ...
}

Что проверить

Перед interface не должно быть JSX (return ( ... без закрывающей скобки).

Проверь последние 20 строк перед 154: возможно, у тебя компонент возвращает JSX и ты забыл }) или </div>.

interface всегда должен быть на верхнем уровне модуля (не внутри function).

📌 Совет: чтобы быстро локализовать — попробуй временно вырезать интерфейс целиком. Если билд пойдёт, значит точно была проблема с позицией объявления.